// Common
export const DEFAULT_CLASSNAME = 'react-apple-tree';
export const LIBRARY_SLUG = 'REACT_APPLE_TREE';

// Theme
export const DEFAULT_ROW_HEIGHT = 62;
export const DEFAULT_SCAFFOLD_BLOCK_PX_WIDTH = 44;
export const DEFAULT_SLIDE_REGION_SIZE = 100;

// Drag and Drop
export const DEFAULT_DND_TYPE = `${LIBRARY_SLUG}_ITEM`;
export const SHARED_IS_DRAGGING_NODE_STATE = `${LIBRARY_SLUG}_SHARED_IS_DRAGGING_NODE_STATE`;
